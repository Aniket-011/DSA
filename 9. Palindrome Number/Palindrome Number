class Solution {
    public boolean isPalindrome(int x) {
        int num =x;
        int result=0;
    while(x>0){
        int res = x%10;
        result = (result*10)+res;
        x= x/10;
    }
        if(num == result)
            return true;
        else
            return false;
    }
}
